// Method to display the card (dark side by default)

// Method : if two same cards are returned do {let them shown}

import React from "react";
import darksidecard from "../../img/card-dark-side.jpg";

function Card() {
    return (
        <img
            src={darksidecard}
        />
    )
}

export default Card;